"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { LogIn, LogOut, ShieldCheck, UserPlus } from "lucide-react";
import { useState } from "react";
type User = { name: string; username: string; initials: string; role: "user" | "admin" };
export function AuthNavigation({ initialUser }: { initialUser: User | null }) { const router = useRouter(); const [user, setUser] = useState<User | null>(initialUser); async function logout() { await fetch("/api/auth/logout", { method: "POST" }); setUser(null); router.push("/login"); router.refresh(); } if (user) return <div className="mt-auto rounded-[22px] bg-sand p-3">{user.role === "admin" && <Link href="/admin/steder" className="mb-2 flex items-center gap-2 rounded-xl bg-limestone px-2 py-2 text-xs font-extrabold text-pine"><ShieldCheck size={15} />Godkend steder</Link>}<Link href="/profil" className="flex items-center gap-3 rounded-xl p-1"><span className="grid h-10 w-10 place-items-center rounded-full bg-clay text-xs font-extrabold text-white">{user.initials}</span><span className="min-w-0 flex-1"><span className="block truncate text-sm font-extrabold text-ink">{user.name}</span><span className="block truncate text-xs text-muted">@{user.username}</span></span></Link><button onClick={logout} className="mt-2 flex w-full items-center gap-2 rounded-xl px-2 py-2 text-xs font-extrabold text-muted hover:bg-limestone hover:text-ink"><LogOut size={15} />Log ud</button></div>; return <div className="mt-auto space-y-2"><Link href="/login" className="flex h-11 items-center justify-center gap-2 rounded-2xl bg-pine text-sm font-extrabold text-limestone"><LogIn size={17} />Log ind</Link><Link href="/opret" className="flex h-11 items-center justify-center gap-2 rounded-2xl border border-line text-sm font-extrabold text-pine"><UserPlus size={17} />Opret dig</Link></div>; }

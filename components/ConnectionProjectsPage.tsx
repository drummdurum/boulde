import Link from "next/link";
import { Eye, Users } from "lucide-react";
import type { ClimbingProject } from "@/types";

export function ConnectionProjectsPage({ projects }: { projects: ClimbingProject[] }) {
  return <main className="min-h-screen px-4 pb-28 pt-5 sm:px-6 lg:ml-[238px] lg:px-8 lg:pb-10"><div className="mx-auto max-w-[1100px]"><header><p className="text-xs font-extrabold uppercase tracking-[.16em] text-clay">Fra dit netværk</p><h1 className="mt-1 text-3xl font-extrabold tracking-[-.04em]">Synlige projekter</h1><p className="mt-2 text-sm font-semibold text-muted">Projekter og medier, som de klatrere du følger, har valgt at dele.</p></header><section className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{projects.map(project => <Link href={`/projekter/${project.id}`} key={project.id} className="rounded-[22px] border border-line bg-limestone p-5 shadow-soft transition hover:border-moss"><div className="flex items-center gap-2 text-xs font-extrabold text-muted"><Users size={15} />{project.owner?.name} · @{project.owner?.username}</div><h2 className="mt-3 text-lg font-extrabold">{project.name}</h2><p className="mt-1 text-sm font-semibold text-muted">{project.location} · {project.grade}</p><div className="mt-4 h-1.5 overflow-hidden rounded-full bg-sand"><div className="h-full rounded-full bg-ochre" style={{ width: `${project.progress}%` }} /></div><p className="mt-2 flex items-center justify-between text-xs font-bold text-muted"><span>{project.status}</span><span>{project.progress}%</span></p></Link>)}</section><aside className="mt-8 flex items-center gap-3 rounded-[22px] border border-dashed border-line p-5 text-sm font-semibold text-muted"><Eye size={20} className="text-clay" />Dine egne projekter er private, indtil du aktivt gør dem synlige.</aside></div></main>;
}
